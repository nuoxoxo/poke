import { defineConfig } from 'astro/config';

// https://astro.build/config

export default defineConfig({
  site: 'https://nuoxoxo.github.io',
  base: '/pokemon', // don't set if no path after `io/`
})
